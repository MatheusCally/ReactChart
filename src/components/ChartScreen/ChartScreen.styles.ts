import { StyleSheet } from "react-native"

export const Style = StyleSheet.create({
    header:{
       borderWidth: 1,
       borderColor: 'black',
        margin: 16,
        maxWidth: 1000
    }
 })